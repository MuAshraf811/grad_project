import 'package:flutter/cupertino.dart';

import '../../../home/view/product_details_view.dart';

class ChatAppBar extends StatelessWidget {
  const ChatAppBar({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const CustomAppBar(
      title: 'Osama Askar',
    );
  }
}
